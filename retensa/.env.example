# Backend secrets — never put these in frontend/.env or VITE_* vars
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Frontend (optional). Leave empty to use the Vite /api proxy to localhost:8000
# VITE_API_BASE_URL=http://localhost:8000
